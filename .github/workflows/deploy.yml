name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build blog
        run: pnpm --filter blog run build
        env:
          VITE_BASE_URL: /

      - name: Build knowledge-base
        run: pnpm --filter knowledge-base run build
        env:
          VITEPRESS_BASE: /kb/
          VITEPRESS_BLOG_URL: /

      - name: Merge dist directories
        run: |
          mkdir -p dist
          mkdir -p dist/kb
          cp -r apps/blog/dist/* dist/
          cp -r apps/knowledge-base/.vitepress/dist/* dist/kb/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: 'dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
