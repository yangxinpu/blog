import { ssrRenderAttrs } from 'vue/server-renderer';
import { useSSRContext } from 'vue';
import { _ as _export_sfc } from './plugin-vue_export-helper.1tPrXgE0.js';
const __pageData = JSON.parse(
  '{"title":"React Principles","description":"","frontmatter":{},"headers":[],"relativePath":"en/React/React原理.md","filePath":"en/React/React原理.md"}'
);
const _sfc_main = { name: 'en/React/React原理.md' };
function _sfc_ssrRender(
  _ctx,
  _push,
  _parent,
  _attrs,
  $props,
  $setup,
  $data,
  $options
) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="react-principles" tabindex="-1">React Principles <a class="header-anchor" href="#react-principles" aria-label="Permalink to &quot;React Principles&quot;">​</a></h1><h3 id="fiber-architecture" tabindex="-1">Fiber Architecture <a class="header-anchor" href="#fiber-architecture" aria-label="Permalink to &quot;Fiber Architecture&quot;">​</a></h3><p>The original React architecture was synchronous rendering (stack reconciler, through tree structure + recursive comparison of old and new DOM), meaning all DOM updates and calculations were completed in the main thread until all tasks finished; this process couldn&#39;t be interrupted, so if complex calculations or long synchronous tasks occurred during rendering, the UI would freeze, resulting in poor user experience;</p><p>Fiber architecture splits tasks into multiple small units (Fiber nodes), so React can pause current tasks during rendering, and dynamically adjust task execution order based on different task priorities, ensuring critical tasks complete as soon as possible without affecting user experience due to long calculations;</p><ul><li><p>Fiber Node: Each Fiber node represents a task unit (a Fiber node usually corresponds to a component), containing component state, UI element information, and other associated tasks; each node stores important properties like <code>effectTag</code> (indicating DOM update type), <code>nextEffect</code> (next update node), <code>return</code> (parent node pointer), Hook chain;</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>//Fiber node structure</span></span>
<span class="line"><span>type Fiber = {</span></span>
<span class="line"><span>  tag: WorkTag;              // Type (Function component / Class component / DOM)</span></span>
<span class="line"><span>  type: any;                 // Component definition</span></span>
<span class="line"><span>  stateNode: any;            // DOM or instance</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  return: Fiber | null;      // Parent node</span></span>
<span class="line"><span>  child: Fiber | null;       // Child node</span></span>
<span class="line"><span>  sibling: Fiber | null;     // Sibling node</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  pendingProps: any;</span></span>
<span class="line"><span>  memoizedProps: any;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  memoizedState: any;        // hooks/state</span></span>
<span class="line"><span>  updateQueue: any;          // Update queue</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  alternate: Fiber | null;   // Double buffering pointer</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  flags: Flags;              // Side effect flags</span></span>
<span class="line"><span>  subtreeFlags: Flags;</span></span>
<span class="line"><span>}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br></div></div></li></ul><p>Fiber workflow:</p><ol><li><p>Initialization Phase:</p><ul><li><p>React starts rendering component tree, creating a Fiber node for each component; each Fiber node represents a component and holds that component&#39;s state, virtual DOM, and context information;</p></li><li><p>Fiber tree construction: React builds a Fiber tree based on virtual DOM structure;</p><ul><li>Parent-child relationship: A Fiber node&#39;s <code>return</code> pointer points to its parent node, so React can backtrack to parent node when needed;</li><li>Sibling relationship: Each node&#39;s <code>next</code> pointer points to its sibling node, enabling React to traverse nodes at the same level in order;</li></ul><p>After creating these nodes, React puts them into work queue, ready for next phase processing;</p></li></ul></li><li><p>Scheduling Phase: Also called reconciliation, diff phase</p><ul><li>Task scheduling and priority: Each task (Fiber node) has a priority, React&#39;s scheduler dynamically decides when to execute which tasks based on these priorities, and can pause current task to execute higher priority tasks: <ol><li>Time slicing: It splits long tasks into small fragments and executes them gradually across multiple browser frames (a cycle of browser rendering page), thus avoiding blocking main thread;</li><li>Task interruption: If higher priority task needs processing, React can interrupt current task and immediately execute high priority task;</li></ol></li><li>Work loop: In scheduling phase, React traverses Fiber nodes and executes reconciliation process. Main tasks of reconciliation are: <ol><li>Calculate new virtual DOM: React calculates each component&#39;s update based on current state and props, generating new virtual DOM;</li><li>Compare virtual DOM: React compares new virtual DOM with old virtual DOM, uses Diff algorithm to find differences, and sets <code>effectTag</code> for parts needing update;</li></ol></li></ul></li><li><p>Commit Phase:</p><ul><li><p>React&#39;s commit phase executes synchronously and cannot be interrupted, completing in one go. It can be subdivided into three sub-phases:</p><ol><li>before mutation: Preparation phase before DOM changes</li><li>mutation: Execute real DOM updates (insert, delete, update)</li><li>layout: Synchronously execute <code>useLayoutEffect</code>, at this point DOM has been updated but not yet painted</li></ol><p>Additionally, <code>useEffect</code> belongs to passive effects, doesn&#39;t execute synchronously in commit phase, but is executed asynchronously by scheduler after browser completes painting</p></li></ul></li></ol><p>Double Buffering Mechanism:</p><p>By first calculating a new virtual DOM tree and comparing with current tree, finally synchronizing updates to real DOM; because React always maintains two Fiber trees during updates, it can compare, interrupt or resume operations at any time;</p><ol><li>Create new virtual DOM tree: When React receives state update or props change, it doesn&#39;t directly modify current virtual DOM tree, but generates a new virtual DOM tree based on new state or props. This new tree is usually called <code>Next Tree</code>;</li><li>Compare virtual DOM trees: React uses virtual DOM&#39;s Diff algorithm to compare differences between <code>Current Tree</code> and <code>Next Tree</code>, calculating minimal update path. This process is called reconciliation;</li><li>Apply updates: In reconciliation phase, React calculates which parts need updating and generates an update plan. This plan is saved in <code>Next Tree</code>. Only after all updates are calculated and confirmed correct, React applies updates to real DOM;</li><li>Switch render tree: After updates complete, React replaces <code>Next Tree</code> (new virtual DOM tree) as <code>Current Tree</code>, using new virtual DOM tree as basis for next render;</li></ol><h3 id="diff-algorithm" tabindex="-1">Diff Algorithm <a class="header-anchor" href="#diff-algorithm" aria-label="Permalink to &quot;Diff Algorithm&quot;">​</a></h3><p>Core mechanism for efficiently updating DOM; it calculates minimal DOM operations by comparing differences between old and new virtual DOM trees, thus avoiding performance loss from full DOM updates;</p><p>React Diff adopts depth-first, level-by-level traversal strategy, starting from root node and comparing level by level:</p><ul><li>Tree Diff (Level comparison): Only compares nodes at same level, doesn&#39;t compare across levels. If node moves across levels, React deletes old node and creates new node, rather than moving node;</li><li>Component Diff: Different type components, directly destroy old component, create new component; Same type components: Update component props, recursively compare child nodes;</li><li>Element Diff: For child element lists at same level, is the core and difficulty of Diff algorithm, divided into three situations: <ol><li>List comparison without key: React defaults to using index as key, has serious performance issues (when inserting elements at list head/middle, all subsequent elements are re-rendered, component state may be misplaced);</li><li>List comparison with key: Establish mapping relationship between old and new nodes through key, achieving efficient add/delete/move operations;</li><li>List Diff double-end comparison optimization: Compare head and tail nodes of old and new lists, find reusable nodes and mark move positions, finally process remaining nodes, greatly reducing move operations;</li></ol></li></ul><h3 id="reactivity" tabindex="-1">Reactivity <a class="header-anchor" href="#reactivity" aria-label="Permalink to &quot;Reactivity&quot;">​</a></h3><p>React&#39;s reactivity is not automatic dependency collection like Vue (based on Proxy/getter-setter), but based on a more explicit, controllable model: State changes → Trigger re-render (render) → Generate new UI → Diff → Minimize DOM updates;</p><ul><li>Reactive trigger sources are only State / Props</li><li>Explicit updates (call API to modify state), no automatic dependency collection, component-level re-render;</li></ul><h3 id="react-vs-vue-rendering-principles" tabindex="-1">React vs Vue Rendering Principles <a class="header-anchor" href="#react-vs-vue-rendering-principles" aria-label="Permalink to &quot;React vs Vue Rendering Principles&quot;">​</a></h3><p><strong>Virtual DOM and Diffing</strong></p><ul><li><strong>React</strong>: Uses <strong>JSX</strong> (JavaScript + XML) syntax to describe component structure. Each component returns a virtual DOM through <code>render</code> method. React performs diffing based on new virtual DOM and current virtual DOM, then efficiently updates virtual DOM through <strong>Fiber architecture</strong>. React&#39;s Diff algorithm optimizes performance by splitting component tree into work units, making minimal updates based on node <strong>key</strong> and <strong>type</strong>.</li><li><strong>Vue</strong>: Vue also uses virtual DOM and diffing, but its implementation is relatively simple. Vue&#39;s <strong>reactivity system</strong> (based on <code>getter</code> and <code>setter</code>) triggers view updates by intercepting data changes, working with React&#39;s virtual DOM. Vue&#39;s Diff algorithm prioritizes comparing node differences from <strong>key</strong> and <strong>tag</strong>, but compared to React, Vue&#39;s virtual DOM updates may be simpler, Vue doesn&#39;t have work unit management like React&#39;s Fiber.</li></ul><p><strong>Reactive Data and Dependency Tracking</strong></p><ul><li><strong>React</strong>: React&#39;s updates are based on <strong>state</strong> and <strong>props</strong> changes. When component&#39;s state or props change, React triggers re-render. React itself doesn&#39;t have built-in reactive data system, it updates state through <code>setState</code>, and re-render triggering is controlled by virtual DOM comparison and scheduling system; When a component&#39;s <strong><code>state</code></strong> or <strong><code>props</code></strong> change, only that component and its downstream components are re-rendered;</li><li><strong>Vue</strong>: Vue is reactive, it tracks data changes through data proxy (reactivity system). Vue intercepts access to data through getter and setter on component&#39;s data object, thus implementing dependency tracking. When data changes, Vue knows which components need updating, avoiding unnecessary re-renders. Therefore, Vue usually doesn&#39;t need to do full virtual DOM comparison like React when data changes, it can directly and precisely know which parts need updating;</li></ul><p><strong>Rendering Process: Synchronous vs Asynchronous</strong></p><ul><li><strong>React</strong>: React 16 introduced <strong>Fiber architecture</strong>, making rendering process support asynchronous operations. React splits rendering tasks into multiple small work units (Fiber), these tasks execute across multiple browser frames. React uses <strong>time-slicing</strong> technology to allocate CPU time, ensuring high-priority tasks (like user interaction) execute first, avoiding long blocking. This enables React to respond more smoothly to user input when handling complex UI updates.</li><li><strong>Vue</strong>: Vue defaults to <strong>synchronous rendering</strong>, meaning when data changes, it immediately executes virtual DOM update and reflects on UI. However, Vue also provides <strong>asynchronous rendering</strong> support, mainly through <code>nextTick</code> method to implement delayed updates. Vue defers update tasks to next tick through event loop queue, thus avoiding updating DOM in same round of events, but Vue&#39;s asynchronous rendering mechanism isn&#39;t a multi-task scheduling system based on Fiber architecture like React;</li></ul><p><strong>Update Mechanism: Batch Updates vs Single Updates</strong></p><ul><li><strong>React</strong>: React uses <strong>batch updates</strong> when updating, merging multiple <code>setState</code> operations into one render task through scheduler queue. React schedules based on each task&#39;s priority, merging multiple updates into same batch, reducing unnecessary re-renders. This batch update mechanism makes React&#39;s UI updates more efficient.</li><li><strong>Vue</strong>: Vue also uses batch update mechanism, especially in multiple reactive data updates, Vue merges multiple data changes into one update task. Vue delays DOM updates through <code>nextTick</code> for batch updates in next event loop. Vue&#39;s mechanism makes view updates not execute repeatedly when reactive data is updated multiple times in short time.</li></ul><p><strong>Performance Optimization: Differentiation Strategy</strong></p><ul><li><strong>React</strong>: React uses <strong>virtual DOM</strong> and <strong>Fiber</strong> architecture to optimize performance. During virtual DOM comparison, React minimizes updates, and improves performance through asynchronous rendering and time-slicing technology. React also provides <code>shouldComponentUpdate</code> and <code>React.memo</code> methods, allowing developers to control when re-render is needed.</li><li><strong>Vue</strong>: Vue&#39;s performance optimization relies on its <strong>reactivity system</strong> and <strong>virtual DOM</strong>. Vue can avoid unnecessary rendering through precise dependency tracking. When data changes, Vue updates only the parts that need changing without re-rendering entire component. Vue also provides <strong>computed</strong> and <strong>watchers</strong> mechanisms to optimize view updates and reactive data flow.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add(
    'en/React/React原理.md'
  );
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const React__ = /* @__PURE__ */ _export_sfc(_sfc_main, [
  ['ssrRender', _sfc_ssrRender],
]);
export { __pageData, React__ as default };
